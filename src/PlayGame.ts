import 'phaser';
import { Question } from "./objects/Question";
import { GameData, shuffleArray } from "./utils";

export class PlayGame extends Phaser.Scene {
   

     constructor() {super("PlayGame");}
    
     preload(): void {
     }
     create(): void {
     }

     update(): void {

     }
 }
